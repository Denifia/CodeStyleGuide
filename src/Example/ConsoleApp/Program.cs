// !! Below is what FAILS the style rules !!

// Constants should be PascalCase
private const string myConst = "";

// Static should start with s_
private static string _myStatic = "";

// Use var if type is obvious
string s = "";

// Don't have more than one blank line in a row


// Use brackets for if statements
if (true)
    Console.WriteLine();
